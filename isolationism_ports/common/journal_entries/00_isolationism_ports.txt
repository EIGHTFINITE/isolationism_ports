je_isolationism_ports = {
	icon = "gfx/interface/icons/company_charter_icons/trade_charter.dds"

	group = je_group_crises

	is_shown_when_inactive = {
		NOT = { has_variable = isolationism_ports_active }
	}

	possible = {
		hidden_trigger = { NOT = { has_variable = isolationism_ports_active } }
	}

	immediate = {
		if = {
			limit = {
				NOT = { has_variable = isolationism_ports_active }
			}
			trigger_event = { id = isolationism_ports.1 }
		}
	}

	complete = {
		has_variable = isolationism_ports_active
	}

	weight = 0
}
